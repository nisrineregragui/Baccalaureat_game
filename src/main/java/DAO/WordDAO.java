package DAO;

public class WordDAO {

    public boolean exists(String word, int category_id) {
        return true; // after configuring MySQl change this Logic
    }

    public void save(String word, int category_id) {
        // logique d'insertion dans la BD set(word isvalid=true)
        // }
    }
}
