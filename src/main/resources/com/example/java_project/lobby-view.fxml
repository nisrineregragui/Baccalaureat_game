<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.example.java_project.LobbyController" styleClass="root" stylesheets="@styles.css" alignment="CENTER" spacing="20">
    <padding><Insets top="40" bottom="40" left="40" right="40"/></padding>

    <!-- Header Section -->
    <VBox alignment="CENTER" spacing="5">
        <Label text="SALLE D'ATTENTE" styleClass="title-label"/>
        <Label fx:id="connectionStatusLabel" text="En attente des copains..." styleClass="label" style="-fx-font-style: italic; -fx-text-fill: #555;"/>
    </VBox>

    <!-- Main Content: Code & Players -->
    <HBox spacing="30" alignment="CENTER" VBox.vgrow="ALWAYS">
        <!-- Left: Game Code (if hosting) or Info -->
        <VBox styleClass="card" spacing="15" alignment="CENTER" minWidth="300">
             <Label text="CODE DE LA PARTIE" styleClass="header-label" style="-fx-font-size: 18px;"/>
             <Label fx:id="codeLabel" text="..." styleClass="letter-display" style="-fx-font-size: 40px;"/>
             <Label text="(Partage ça avec tes amis !)" styleClass="label" style="-fx-font-size: 14px; -fx-text-fill: #888;"/>
        </VBox>

        <!-- Right: Player List -->
        <VBox styleClass="card" spacing="10" alignment="TOP_CENTER" minWidth="300" prefHeight="300" HBox.hgrow="ALWAYS">
            <Label text="JOUEURS CONNECTÉS" styleClass="header-label" style="-fx-font-size: 18px;"/>
            <ListView fx:id="playerList" styleClass="list-view" VBox.vgrow="ALWAYS"/>
        </VBox>
    </HBox>
    
    <!-- Settings (Host Only) -->
    <!-- Settings (Host Only) -->
    <HBox fx:id="hostControls" spacing="20" alignment="CENTER" visible="false">
        <VBox styleClass="card" spacing="10" alignment="CENTER">
            <Label text="⏱ Temps par tour" styleClass="header-label" style="-fx-font-size: 18px;"/>
            <ComboBox fx:id="durationComboBox" promptText="Choisir le temps" styleClass="text-field" prefWidth="250"/>
        </VBox>
        <!-- Categories Selection -->
         <VBox fx:id="categoriesContainer" styleClass="card" spacing="5" visible="false" managed="false" VBox.vgrow="ALWAYS">
             <Label text="📚 Catégories" styleClass="header-label" style="-fx-font-size: 18px;"/>
             <ListView fx:id="categoriesListView" styleClass="list-view" VBox.vgrow="ALWAYS"/>
        </VBox>
    </HBox>

    <!-- Footer Action -->
    <Button text="DÉMARRER !" fx:id="startButton" onAction="#onStartGameClick" visible="false" styleClass="button, secondary-button" style="-fx-font-size: 24px; -fx-padding: 10 50;"/>

</VBox>
